# 📚 גישה פשוטה: MERGE של שני הספרים

## מה אתה רוצה?

**התסריט = הטקסט מהספרים עצמם**  
**AI רק עושה MERGE הגיוני של שני הספרים לפי ציר זמן**

---

## ✅ יש לך שתי אפשרויות:

### אפשרות 1: הגישה המורכבת (כבר קיימת) 🔧

**מה זה עושה:**
- מזהה סצנות דומות משני הספרים (לפי שנה, מקום, דמויות)
- ממזג רק סצנות קשורות
- יוצר מבנה JSON מורכב עם metadata

**סקריפטים:**
```bash
python extract_scenes.py          # חילוץ סצנות
python merge_scenes_from_books.py # מיזוג סצנות דומות
```

**תוצאה:** `merged_scenes.json` - סצנות ממוזגות עם metadata

**מתי להשתמש:** אם אתה רוצה לזהות סצנות ספציפיות ולמזג רק אותן

---

### אפשרות 2: הגישה הפשוטה (חדש!) ⭐

**מה זה עושה:**
- לוקח את כל הטקסט משני הספרים
- ממזג אותם לפי ציר זמן (1900-1921)
- יוצר טקסט אחד רציף עם ציון מקור

**סקריפט:**
```bash
python merge_books_simple.py
```

**תוצאה:** `merged_books_text.txt` - טקסט אחד ממוזג לפי ציר זמן

**מתי להשתמש:** אם אתה רוצה שהתסריט יהיה הטקסט מהספרים עצמם

---

## 🎯 איזו גישה מתאימה לך?

### אם אתה רוצה:
- ✅ **התסריט = הטקסט מהספרים עצמם**
- ✅ **AI רק עושה MERGE הגיוני**
- ✅ **פשוט וישיר**

**→ השתמש ב-`merge_books_simple.py`** ⭐

### אם אתה רוצה:
- ✅ לזהות סצנות ספציפיות
- ✅ למזג רק סצנות קשורות
- ✅ מבנה JSON מורכב עם metadata

**→ השתמש ב-`extract_scenes.py` + `merge_scenes_from_books.py`**

---

## 📋 איך להשתמש בגישה הפשוטה:

### שלב 1: MERGE של שני הספרים (Python)
```bash
cd movie/scripts
python merge_books_simple.py
```

**תוצאה:**
- `movie/output/scripts/merged_books_text.txt` - הטקסט הממוזג
- `movie/output/scripts/merged_books_structure.json` - המבנה (למעקב)

**מה זה עושה:**
1. קורא את כל העמודים משני הספרים
2. מזהה שנים (או מעריך לפי פרקים)
3. מסדר לפי ציר זמן (1900-1921)
4. יוצר טקסט אחד עם ציון מקור לכל חלק

**דוגמה לתוצאה:**
```
============================================================
שנה: 1904
============================================================

[מבית מרקובסקי - באר־טוביה, עמוד 14]
הטקסט מהספר...

[משדות ראשונים - ראשית, עמוד 9]
הטקסט מהספר...

============================================================
שנה: 1905
============================================================
...
```

---

### שלב 2: שימוש ב-AI (אופציונלי - רק אם תרצה!)

**⚠️ חשוב:** Python כבר עושה MERGE טוב! AI רק משפר (אופציונלי).

**אפשרות A: להשתמש בטקסט כמו שהוא** ⭐ (מומלץ!)
- הטקסט הממוזג מ-Python הוא התסריט
- השתמש בו ישירות ליצירת קריינות ווידאו
- **זה כבר מוכן!**

**אפשרות B: לבקש מ-AI לעשות MERGE טוב יותר** (רק אם יש כפילויות)
- פתח את `merged_books_text.txt`
- שלח ל-ChatGPT/Claude:
  ```
  קח את הטקסט הזה (שני ספרים ממוזגים לפי ציר זמן) 
  ועשה MERGE הגיוני יותר - הסר כפילויות, 
  חבר חלקים קשורים, שמור על הסדר הכרונולוגי.
  התסריט צריך להיות הטקסט מהספרים עצמם, 
  בלי יצירה חדשה.
  ```

**👉 קרא עוד:** [MERGE_EXPLANATION.md](MERGE_EXPLANATION.md) - הסבר מפורט על מי עושה מה

---

## 🔄 השוואה בין הגישות:

| קריטריון | גישה מורכבת | גישה פשוטה |
|----------|-------------|------------|
| **מהירות** | איטי יותר | מהיר |
| **פשטות** | מורכב | פשוט |
| **תוצאה** | JSON מובנה | טקסט פשוט |
| **זיהוי סצנות** | כן | לא |
| **MERGE לפי זמן** | כן | כן |
| **התסריט = הספר** | לא בדיוק | כן! |

---

## 💡 המלצה שלי:

**התחל עם הגישה הפשוטה!** ⭐

1. הרץ `merge_books_simple.py`
2. בדוק את `merged_books_text.txt`
3. אם זה נראה טוב - השתמש בזה!
4. אם אתה רוצה שיפורים - שלח ל-AI לעשות MERGE טוב יותר

**למה?**
- ✅ פשוט יותר
- ✅ מהיר יותר
- ✅ התסריט = הטקסט מהספרים (כמו שאתה רוצה)
- ✅ AI רק עושה MERGE הגיוני (כמו שאתה רוצה)

---

## 📝 סיכום:

**הגישה הפשוטה (`merge_books_simple.py`):**
- ✅ ממזג את שני הספרים לטקסט אחד לפי ציר זמן
- ✅ התסריט = הטקסט מהספרים עצמם
- ✅ AI רק עושה MERGE הגיוני (אם תרצה)
- ✅ פשוט, מהיר, ישיר

**זה בדיוק מה שאתה רוצה!** 🎯
